# Port the server will listen on locally
PORT=3001

# Environment mode (development or production)
NODE_ENV=development

# Frontend URL for CORS (use your local URL for development)
# When you deploy, you will change this to your Render/Vercel URL
CLIENT_URL=http://localhost:3000